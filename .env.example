OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=

# Overmind platform (required for `overmind start` / `overmind optimize`)
OVERMIND_API_KEY=
OVERMIND_PROJECT_ID=
# OVERMIND_API_URL=https://api.overmindlab.ai

# Used as the optimizer’s analyzer model (and as the default in the model picker).
ANALYZER_MODEL=claude-sonnet-4-6
SYNTHETIC_DATAGEN_MODEL=claude-opus-4-6
