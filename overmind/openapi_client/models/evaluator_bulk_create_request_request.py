# coding: utf-8

"""
    Overbae API

    Backend API for the Overclaw / Overmind platform.

    The version of the OpenAPI document: 2.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from uuid import UUID
from typing import Optional, Set
from typing_extensions import Self

class EvaluatorBulkCreateRequestRequest(BaseModel):
    """
    EvaluatorBulkCreateRequestRequest
    """ # noqa: E501
    project: UUID
    dataset: Optional[UUID] = Field(default=None, description="Optional dataset the specs were generated for. When provided, every spec's variable_mapping is dry-run against real sample units at creation; specs whose evidence bindings resolve to nothing on every sample are quarantined (created disabled) instead of grading blanks.")
    specs: List[Dict[str, Any]] = Field(description="EvaluatorSpec payloads (as returned by the generate-evals endpoint).")
    mode: Optional[StrictStr] = Field(default=None, description="Intended run mode. Threaded into the creation-time binding dry-run so an output-reading binding that is empty only because the dataset has no captured generate run yet is reported 'skipped' (unknown) rather than quarantined.  * `generate` - generate * `existing` - existing")
    __properties: ClassVar[List[str]] = ["project", "dataset", "specs", "mode"]

    @field_validator('mode')
    def mode_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['generate', 'existing']):
            raise ValueError("must be one of enum values ('generate', 'existing')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of EvaluatorBulkCreateRequestRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if dataset (nullable) is None
        # and model_fields_set contains the field
        if self.dataset is None and "dataset" in self.model_fields_set:
            _dict['dataset'] = None

        # set to None if mode (nullable) is None
        # and model_fields_set contains the field
        if self.mode is None and "mode" in self.model_fields_set:
            _dict['mode'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of EvaluatorBulkCreateRequestRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "project": obj.get("project"),
            "dataset": obj.get("dataset"),
            "specs": obj.get("specs"),
            "mode": obj.get("mode")
        })
        return _obj


